<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.

This repository is a Pages Router Next.js project. Put pages in `pages/`, API routes in `pages/api/`, app-wide wrappers in `pages/_app.tsx`, document markup in `pages/_document.tsx`, and global styles in `styles/globals.css`. Do not add App Router files or routes under `app/` unless the project is intentionally migrated.
<!-- END:nextjs-agent-rules -->
